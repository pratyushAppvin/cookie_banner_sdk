# Assets Directory

Place any banner assets here (logos, icons, etc.) if needed.

The SDK fetches most assets dynamically from the backend/CDN, but this directory is available for bundled fallback assets.
